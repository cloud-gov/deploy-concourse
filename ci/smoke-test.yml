---
platform: linux
image_resource:
  type: docker-image
  source:
    repository: 18fgsa/concourse-task

inputs:
- name: concourse-config

run:
  path: concourse-config/ci/smoke-test.sh

params:
  ATC_URL:
  BASIC_AUTH_USERNAME:
  BASIC_AUTH_PASSWORD:
